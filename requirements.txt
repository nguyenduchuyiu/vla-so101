# vla-so101 — direct dependencies (top-level, pinned to the working uv venv).
# Recreate the venv:  uv venv && uv pip install -r requirements.txt
# Full transitive lock not recorded; this lists only what the code imports directly.

# torch (VLA training + inference, MPS/CUDA)
torch==2.13.0
torchvision==0.28.0

# SmolVLM backbone + LoRA
transformers==4.57.6
accelerate==1.14.0
peft==0.19.1
safetensors==0.8.0
num2words==0.5.14

# simulation env
mujoco==3.10.0
so101-nexus==0.4.10
scipy==1.18.0

# data / io
numpy==2.5.1
pillow==12.3.0
pyyaml==6.0.3
tqdm==4.69.0
mediapy==1.2.7          # evaluate_so101.py video output
mmengine==0.10.7        # simvla_datasets fileio

# optional, GPU/Linux only (CUDA; not installable on macOS/MPS)
flash-attn>=2.5.0